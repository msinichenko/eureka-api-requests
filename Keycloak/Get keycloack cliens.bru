meta {
  name: Get keycloack cliens
  type: http
  seq: 10
}

get {
  url: https://keycloak-{{folio}}.{{domain}}/admin/realms/{{realm}}/clients
  body: none
  auth: bearer
}

headers {
  ~Content-Type: application/x-www-form-urlencoded
}

auth:bearer {
  token: {{master-realm-token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
