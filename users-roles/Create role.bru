meta {
  name: Create role
  type: http
  seq: 11
}

post {
  url: https://kong-{{folio}}.{{domain}}/roles
  body: json
  auth: none
}

params:query {
  ~: 
}

headers {
  x-okapi-token: {{okapi-token}}
  x-okapi-tenant: {{realm}}
}

body:json {
  {
    "name": "AdminRole",
    "description": "AdminRole"
  }
}

script:pre-request {
  const res = await bru.runRequest("auth/get-keycloak-tenant-token");
}settings {
  encodeUrl: true
  timeout: 0
}
